# PrismWave 首页阶段 8：响应式与视觉统一实施计划

**范围：** 仅做前七阶段完成后的整体一致性与响应式验收，不重新设计模块，不改歌曲数据、业务逻辑、颜色体系或页面顺序。

## Task 1：建立最终静态布局契约

- [x] 为 Shell、Home、首页四个内容模块和底部播放栏补充最终 XAML 审计测试。
- [x] 验证 Shell 使用内容区 `*` + 播放栏 `Auto` 两行，Home 使用标题 `Auto` +滚动区 `*` 两行。
- [x] 验证首页模块顺序为 Trending、全球热门、编辑精选、流派探索。
- [x] 验证不存在 `Canvas`、负 Margin、ZIndex、超大固定总宽度、最近播放和私人雷达。
- [x] 运行静态测试并记录结果：`HomeFinalLayoutAuditTests` 4/4 通过。

## Task 2：逐档运行检查响应式布局

- [x] 编译并启动最新 WinUI demo，确认默认进入首页。
- [x] 检查导航栏展开和折叠状态，确认内容不被覆盖。
- [x] 检查 1280x720、1440x900、1600x900、1920x1080，并以物理窗口矩形确认尺寸。
- [x] 检查页面顶部、榜单 Hover、页面底部和完整播放栏。
- [x] 每发现一个可复现问题，只修改对应容器，重新编译、运行、截图后再继续。

## Task 3：一致性收尾

- [x] 对照截图核对页面左右边距、标题间距、圆角、字体层级、图标尺寸、Hover 和 Accent 使用。
- [x] 确认垂直滚动条不覆盖刷新按钮，横向内容无永久裁切。
- [x] 确认最后一行可滚动到播放栏上方，播放栏不会覆盖页面。
- [x] 确认折叠态与窄窗口无重叠、文字半行裁切或控件越界。

## Task 4：最终验证与交付

- [x] 运行完整测试与 x64 编译：80/80 tests，0 warnings，0 errors。
- [x] 保存最终截图到 `docs/ui-review/`，并逐张查看。
- [x] 输出逐项验收说明，列明 XAML 容器、Grid 行列和 Margin/Padding。
- [x] 保持最新 demo 运行在首页，供用户直接查看。
